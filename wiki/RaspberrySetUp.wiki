#labels Featured
= Initial raspberry setup =

  * Download Debian Wheezy ([http://www.raspberrypi.org/downloads download])
  * Install it using dd / Win32 diskimager (found on raspberry dl page)
  * Make the first setup of Raspberry (Overclocking it is recommended)
  * Then Run the following lines of code:

{{{
   sudo -s
}}}
{{{
   apt-get update && apt-get upgrade
}}}
{{{   
   apt-get install arduino openjdk-6-jdk samba vftpd
}}}
{{{
   ln -s /dev/ttyACM0 /dev/ttyUSB0
}}}   

= Installing Tomcat =

{{{
sudo -s
wget http://mirrors.axint.net/apache/tomcat/tomcat-7/v7.0.34/bin/apache-tomcat-7.0.34.tar.gz
tar xzf apache-tomcat-7.0.34.tar.gz
cd apache-tomcat-7.0.34/conf
vi tomcat-users.xml
}}}

[http://macgyverdev.blogspot.co.at/2012/08/raspberry-pi-how-to-get-ssh-and-tomcat.html]